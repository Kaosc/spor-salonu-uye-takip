# Maybe
- Dynamic price selection and changing the price of a subscription

# Check-Ins
   - Create a Check-ins collection and it will be only incremented when user checks in with qr code. It will have the following fields:
      interface CheckIn {
         id: string;
         memberUid: string;
         checkInTime: Date;
         checkOutTime?: Date;
      }
