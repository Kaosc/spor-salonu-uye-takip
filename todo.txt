x (on progress) Statistic screen for the role of "ADMIN"
   - This screen will show the statistic of the members and subscriptions.
       x (on progress) - How many members are active or inactive.
       x (on progress) - How many subscriptions are active, paused, or canceled.
       x (on progress) - How many members checked-in today.
       x (on progress) - How many members checked-in this week.
       x (on progress) - How many members checked-in this month.
       x (on progress) - How many members checked-in this year.
       x (on progress) - Total income from subscriptions this month.
       x (on progress) - Total income from subscriptions this year.
       x (on progress) - Total income from subscriptions this month by subscription type.
       x (on progress) - Total income

- Pagging on members list

- Don't assign locker to user if user did not checked in today

- Checkout user by letting staff to scan the qr code of the user. If the user is checked in (and checkOutTime is null), then the user will be checked out and the locker will be unlocked. If there is no check-in record for the user today, then add a new check-in record.

